declare module "expo-react-native-toastify" {
  // Add more specific type declarations here if needed,
  // e.g., for exported functions or components.
  export const ToastManager: any; // Or more specific types if known
  export default Toast;
}
